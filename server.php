<?php
	// Used to initialize errors/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	
	$errors = "";

	// Used to connect my database 'list'////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	$db = mysqli_connect("localhost", "root", "", "todolist");

	// Used to insert a task/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

	if (isset($_POST['submit'])) {
		if (empty($_POST['task'])) {

			//Error Message//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
			
			$errors = "Try adding a task";
		} else {
			$task = $_POST['task'];
			$sql = "INSERT INTO list (task) VALUES ('$task')";
			mysqli_query($db, $sql);
			header('location:'.$_SERVER['PHP_SELF']);
		}
    }

    if (isset($_GET['del_task'])) {
        $id = $_GET['del_task'];
        
        mysqli_query($db, "DELETE FROM list WHERE id='$id' LIMIT 1" );
        header('location: index.php');
    }
    $tasks = mysqli_query($db, "SELECT * FROM list");

    
?>